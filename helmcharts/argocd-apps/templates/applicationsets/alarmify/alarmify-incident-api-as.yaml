apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: alarmify-incident-api
  namespace: argocd
  labels:
    app.kubernetes.io/name: alarmify-incident-api
    app.kubernetes.io/instance: alarmify-incident-api
    app.kubernetes.io/component: applicationset
    app.kubernetes.io/part-of: argocd-apps
    app.kubernetes.io/managed-by: Helm
  annotations:
    argocd.argoproj.io/managed: "true"
spec:
  generators:
    - clusters:
        selector:
          matchExpressions:
            - key: environment
              operator: In
              values:
                - dev
        values:
          environment: dev
          namespace: alarmify-incident-api
  syncPolicy:
    applicationsSync: sync
    preserveResourcesOnDeletion: false

  template:
    metadata:
      name: {{ `"{{name}}-alarmify-incident-api"` }}
      namespace: argocd
      labels:
        app.kubernetes.io/name: alarmify-incident-api
        app.kubernetes.io/instance: alarmify-incident-api
        app.kubernetes.io/component: application
        app.kubernetes.io/part-of: argocd-apps
        app.kubernetes.io/managed-by: Helm
        environment: {{ `"{{values.environment}}"` }}
      annotations:
        argocd.argoproj.io/managed: "true"
    spec:
      project: default
      source:
        repoURL: git@github.com:defyjoy/google-cloud-argocd.git
        targetRevision: HEAD
        path: helmcharts/alarmify/alarmify-incident-api
        helm:
          valueFiles:
            - values.yaml
            - {{ `"values/{{values.environment}}.yaml"` }}
          ignoreMissingValueFiles: true
      destination:
        server: {{ `"{{server}}"` }}
        namespace: {{ `"{{values.namespace}}"` }}
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        managedNamespaceMetadata:
          annotations:
            argocd.argoproj.io/managed: "true"
          labels:
            name: {{ `"{{values.namespace}}"` }}
            environment: {{ `"{{values.environment}}"` }}
        retry:
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
          limit: 5
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
          - PruneLast=true
          - PrunePropagationPolicy=foreground
      revisionHistoryLimit: 3
