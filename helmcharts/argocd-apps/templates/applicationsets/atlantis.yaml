apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: atlantis
  namespace: argocd
  labels:
    app.kubernetes.io/name: atlantis
    app.kubernetes.io/instance: atlantis
    app.kubernetes.io/component: applicationset
    app.kubernetes.io/part-of: argocd-apps
    app.kubernetes.io/managed-by: Helm
  annotations:
    argocd.argoproj.io/managed: "true"
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          atlantis: "true"
  syncPolicy:
    applicationsSync: sync
    preserveResourcesOnDeletion: false

  template:
    metadata:
      name: {{ `"{{name}}-atlantis"` }}
      namespace: argocd
      labels:
        app.kubernetes.io/name: atlantis
        app.kubernetes.io/instance: atlantis
        app.kubernetes.io/component: application
        app.kubernetes.io/part-of: argocd-apps
        app.kubernetes.io/managed-by: Helm
        cluster: {{ `"{{name}}"` }}
        environment: production
      annotations:
        argocd.argoproj.io/managed: "true"
    spec:
      project: default
      source:
        repoURL: git@github.com:defyjoy/google-cloud-argocd.git
        targetRevision: HEAD
        path: helmcharts/atlantis
        helm:
          valueFiles:
            - values.yaml
          parameters:
            - name: "atlantis.ingress.hosts[0].host"
              value: {{ `"atlantis.{{name}}.jrclabs.xyz"` }}
            - name: "atlantis.ingress.tls[0].secretName"
              value: {{ `"atlantis-{{name}}-tls"` }}
            - name: "atlantis.ingress.tls[0].hosts[0]"
              value: {{ `"atlantis.{{name}}.jrclabs.xyz"` }}
      destination:
        server: {{ `"{{server}}"` }}
        namespace: atlantis
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        managedNamespaceMetadata:
          annotations:
            argocd.argoproj.io/managed: "true"
          labels:
            name: "atlantis"
            pod-security.kubernetes.io/audit: "restricted"
            pod-security.kubernetes.io/enforce: "restricted"
            pod-security.kubernetes.io/warn: "restricted"
        retry:
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
          limit: 5
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
          - PruneLast=true
          - PrunePropagationPolicy=foreground
      revisionHistoryLimit: 3
