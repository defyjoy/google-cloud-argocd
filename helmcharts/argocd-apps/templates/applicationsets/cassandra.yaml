apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cassandra
  namespace: argocd
  labels:
    app.kubernetes.io/name: cassandra
    app.kubernetes.io/instance: cassandra
    app.kubernetes.io/component: applicationset
    app.kubernetes.io/part-of: argocd-apps
    app.kubernetes.io/managed-by: Helm
  annotations:
    argocd.argoproj.io/managed: "true"
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          cassandra: "true"
  syncPolicy:
    applicationsSync: sync
    preserveResourcesOnDeletion: false

  template:
    metadata:
      name: {{ `"{{name}}-cassandra"` }}
      namespace: argocd
      labels:
        app.kubernetes.io/name: cassandra
        app.kubernetes.io/instance: cassandra
        app.kubernetes.io/component: application
        app.kubernetes.io/part-of: argocd-apps
        app.kubernetes.io/managed-by: Helm
        cluster: {{ `"{{name}}"` }}
        environment: production
      annotations:
        argocd.argoproj.io/managed: "true"
    spec:
      project: default
      source:
        repoURL: git@github.com:defyjoy/google-cloud-argocd.git
        targetRevision: HEAD
        path: helmcharts/cassandra
        helm:
          valueFiles:
            - values.yaml
          parameters:
            - name: "cassandra.ingress.hosts[0].host"
              value: {{ `"cassandra.{{name}}.jrclabs.xyz"` }}
            - name: "cassandra.ingress.tls[0].secretName"
              value: {{ `"cassandra-{{name}}-tls"` }}
            - name: "cassandra.ingress.tls[0].hosts[0]"
              value: {{ `"cassandra.{{name}}.jrclabs.xyz"` }}
      destination:
        server: {{ `"{{server}}"` }}
        namespace: cassandra
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        managedNamespaceMetadata:
          annotations:
            argocd.argoproj.io/managed: "true"
          labels:
            name: "cassandra"
            pod-security.kubernetes.io/audit: "restricted"
            pod-security.kubernetes.io/enforce: "restricted"
            pod-security.kubernetes.io/warn: "restricted"
        retry:
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
          limit: 5
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
          - PruneLast=true
          - PrunePropagationPolicy=foreground
      revisionHistoryLimit: 3
