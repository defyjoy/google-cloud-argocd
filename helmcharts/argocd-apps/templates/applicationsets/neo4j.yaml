apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: neo4j
  namespace: argocd
  labels:
    app.kubernetes.io/name: neo4j
    app.kubernetes.io/instance: neo4j
    app.kubernetes.io/component: applicationset
    app.kubernetes.io/part-of: argocd-apps
    app.kubernetes.io/managed-by: Helm
  annotations:
    argocd.argoproj.io/managed: "true"
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          neo4j: "true"
  syncPolicy:
    applicationsSync: sync
    preserveResourcesOnDeletion: false

  template:
    metadata:
      name: {{ `"{{name}}-neo4j"` }}
      namespace: argocd
      labels:
        app.kubernetes.io/name: neo4j
        app.kubernetes.io/instance: neo4j
        app.kubernetes.io/component: application
        app.kubernetes.io/part-of: argocd-apps
        app.kubernetes.io/managed-by: Helm
        cluster: {{ `"{{name}}"` }}
        environment: production
      annotations:
        argocd.argoproj.io/managed: "true"
    spec:
      project: default
      source:
        repoURL: git@github.com:defyjoy/argocd-google-cloud.git
        targetRevision: HEAD
        path: helmcharts/neo4j
        helm:
          valueFiles:
            - values.yaml
          parameters:
            - name: "neo4j.ingress.hosts[0].host"
              value: {{ `"neo4j.{{name}}.workquark.org"` }}
            - name: "neo4j.ingress.tls[0].secretName"
              value: {{ `"neo4j-{{name}}-tls"` }}
            - name: "neo4j.ingress.tls[0].hosts[0]"
              value: {{ `"neo4j.{{name}}.workquark.org"` }}
      destination:
        server: {{ `"{{server}}"` }}
        namespace: neo4j
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        managedNamespaceMetadata:
          annotations:
            argocd.argoproj.io/managed: "true"
          labels:
            name: "neo4j"
            pod-security.kubernetes.io/audit: "restricted"
            pod-security.kubernetes.io/enforce: "restricted"
            pod-security.kubernetes.io/warn: "restricted"
        retry:
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
          limit: 5
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
          - PruneLast=true
          - PrunePropagationPolicy=foreground
      revisionHistoryLimit: 3
