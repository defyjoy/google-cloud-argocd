apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: opentelemetry-kube-stack
  namespace: argocd
  labels:
    app.kubernetes.io/name: opentelemetry-kube-stack
    app.kubernetes.io/managed-by: Helm
  annotations:
    argocd.argoproj.io/managed: "true"
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          opentelemetry-kube-stack: "true"
  syncPolicy:
    applicationsSync: sync
    preserveResourcesOnDeletion: false

  template:
    metadata:
      name: {{ `"{{name}}-opentelemetry-kube-stack"` }}
      namespace: argocd
      labels:
        app.kubernetes.io/name: opentelemetry-kube-stack
        cluster: {{ `"{{name}}"` }}
        environment: production
    spec:
      project: default
      source:
        repoURL: git@github.com:defyjoy/google-cloud-argocd.git
        targetRevision: HEAD
        path: helmcharts/opentelemetry-kube-stack
        helm:
          valueFiles:
            - values.yaml
      destination:
        server: {{ `"{{server}}"` }}
        namespace: opentelemetry-kube-stack
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        managedNamespaceMetadata:
          labels:
            name: "opentelemetry-kube-stack"
        retry:
          backoff:
            duration: 5s
            maxDuration: 3m
          limit: 5
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
          - PruneLast=true
          - PrunePropagationPolicy=foreground
      revisionHistoryLimit: 3
