apiVersion: v2
name: cilium
description: >-
  Wrapper chart for Cilium as the cluster CNI + kube-proxy replacement + LoadBalancer
  (LB-IPAM / L2 announcement), replacing Flannel and kube-proxy.
  See alarmify-docs: docs/cilium/cilium-migration-plan.md and
  docs/infrastructure/adr-009-cilium-cni-loadbalancer-migration.md.

type: application

# Wrapper chart version (this repo's packaging). Bump on template/values changes here.
version: 0.1.0

# Pinned Cilium version. 1.20.0-rc.1 is the first Cilium line that supports Kubernetes 1.35
# (stable 1.19.x tops out at 1.34). RC is acceptable on the dev canary only; management waits
# for 1.20.0 GA per the migration plan (Phases 6-9).
appVersion: "1.20.0-rc.1"

dependencies:
  - name: cilium
    version: "1.20.0-rc.1"
    repository: https://helm.cilium.io
    condition: enabled
