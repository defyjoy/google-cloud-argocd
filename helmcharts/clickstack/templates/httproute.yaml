{{- if .Values.httproute.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: clickstack-hyperdx-httproute
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: clickstack
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app: clickstack-hyperdx-httproute
  {{- with .Values.httproute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.httproute.parentRefs }}
  parentRefs:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.httproute.hostnames }}
  hostnames:
    {{- range . }}
    - {{ . | quote }}
    {{- end }}
  {{- end }}
  rules:
    - matches:
        - path:
            type: {{ .Values.httproute.pathType | default "PathPrefix" }}
            value: {{ .Values.httproute.path | default "/" }}
      backendRefs:
        - group: ""
          kind: Service
          name: {{ .Values.httproute.backendServiceName | quote }}
          port: {{ .Values.httproute.backendPort }}
          weight: 1
{{- end }}
