apiVersion: v1
kind: Secret
metadata:
  name: postgresql-credentials
  namespace: cloudnative-pg-system
type: kubernetes.io/basic-auth
stringData:
  username: app_user
  password: changeme-in-production  # Change this in production!
  dbname: alarmify
