apiVersion: v2
name: gke-gateway
description: The cluster's single Gateway (gateway-system/gateway), backed by a GKE-managed Application Load Balancer — the parent every HTTPRoute in this repo attaches to
type: application
version: 0.1.0
appVersion: "1.35"

keywords:
  - gateway-api
  - gke
  - ingress
  - loadbalancer
