apiVersion: v2
name: vault-backup
description: >-
  Daily Vault Raft snapshots to S3 (management only). Uses
  `vault operator raft snapshot save` and uploads via rclone.
  Credentials via ExternalSecret (Vault token + AWS keys).
type: application
version: 0.1.0
appVersion: "1.20.4"
